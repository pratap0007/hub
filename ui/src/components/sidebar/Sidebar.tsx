import { PageSidebar } from '@patternfly/react-core';
import React from 'react';

const PageNav = <div>filter itests</div>;

const Sidebar: React.FC = () => {
  return <PageSidebar nav={PageNav} theme="light" />;
};
export default Sidebar;
